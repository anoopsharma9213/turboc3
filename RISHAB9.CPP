#include<rishab.cpp>
       void main()
       { clrscr();
       char str[80];
      cout<<"enter a string";
       gets(str);
       n=strlen[str]-1;
       for(int i=0;i<strlen(str);i++)
       {
       if (strlen(str)==

