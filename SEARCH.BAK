#include<stdio.h>
#include<conio.h>

void main()
{
 int a[10],num,i,n,k=0;
 clrscr();
 printf("Enter no. of elements: ");
 scanf("%d",&n);
 for(i=0;i<n;i++)
 {printf("Enter Element: ");
  scanf("%d",&a[i]);
 }
 clrscr();
 printf("Enter element to be searched: ");
 scanf("%d",&num);
 for(i=0;i<n;i++)
  if(a[i]==num)
  {printf("\n\n\tElement is at %d position.",i+1);
   k++;
  }
 if(k==0)
  printf("\n\n\t\tElement not found!!!");
 getch();
}